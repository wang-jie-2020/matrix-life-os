# ASCII Life OS v0.3.0 发布说明

> Reset note: this file is a historical pre-reset release note. It is not a current product requirement. Use `docs/project-requirements.md` for the current Matrix Life OS scope and `docs/remediation-plan.md` for cleanup decisions.

> **「你的人生，值得一款操作系统。」**
>
> 这一版不是加功能，是把骨架重新接了一遍。

---

## 概述

v0.3.0 是 ALO 的 **v2.0 GTD 重构版**。我们把原本分散的 Dashboard / 反思库 / 系统页，压缩成符合 GTD 自然流的三页结构：**行动台 → 回顾档案 → 系统**。所有模块被重新归类到「捕捉 / 支撑 / 执行 / 洞察」四个阶段，模块可自由开关，但信息流不再乱跑。

---

## 核心重构

### 1. 三页架构正式落地

- **行动台 (Action Desk)**：收集、澄清、安排、执行。周看板是心脏。
- **回顾档案 (Review Archive)**：反思、能力雷达、能力训练、光荣榜、数据备份。
- **系统 (System)**：模块开关、反思模板、数据体检、版本更新、生存指南。

顶部导航只保留这三个入口， cognitive load 降到最低。

### 2. 模块管理迁入系统页

- 移除了导航栏上的全局「模块选择器」浮层。
- 所有模块开关统一在 **系统 → 功能开关** 中管理。
- 核心模块（收集箱、周看板、OKR、原则、能力、反思档案、光荣榜）默认开启且不可关闭。
- 可选模块（习惯、心情、时间块、灵感仓库）按需启用。

### 3. 彻底移除 Backlog 与 Snapshot

- **Backlog 面板已删除**。 backlog 作为一种「拖延合法化容器」，与 ALO「今日即执行」的信条冲突。
  - 旧 backlog 任务在升级时自动迁移到今天，不丢数据。
  - 收集箱澄清后的任务现在直接进入「今日」。
  - 能力子任务不再提供 autoAddToBacklog 开关。
- **Snapshot / Rollback 功能已删除**。原子写 + `.bak` 备份机制已足够保证数据安全，移除了相关的 IPC、配置、UI 和数据字段，降低维护负担。

### 4. 单向 GTD 行动流

跨模块拖拽现在遵循严格方向：

```
来源区（收集箱 / KR / 灵感 / 娱乐 / 能力子任务）
        ↓ arrange
执行区（周看板 / 时间块 / 日历）
        ↓ complete
回顾区（反思 / 光荣榜）
```

- KR 可拖拽到周看板生成执行 Task，KR 原处保留并标记已安排。
- 灵感、娱乐活动可拖拽到周看板，原条目保留。
- 看板任务**不允许**反向拖回来源区；原则区只读，不接收任何 drop。

### 5. 数据迁移框架 v0.3.0

首次引入自动迁移链。从任意旧版本启动时，ALO 会：

1. 备份当前数据（原子写 + `.bak`）。
2. 按版本链依次执行迁移：
   - 旧 Objective 结构（period-based）→ v2.0 status-based 结构。
   - Task 补齐 `source` / `linkedKrId`，并把遗留的 `BACKLOG` 任务迁移到今日。
   - Reflection 补齐 `linkedObjectiveIds`。
   - Ability 清理 `autoAddToBacklog`。
   - Config 清理 `autoSnapshot` / `lastSnapshotAt`。
3. 标记数据版本为当前版本，避免重复迁移。

### 6. 全新的文案人格

- 新增 `titles-copy.ts`，所有模块标题使用**精简中文 + 黑色幽默**风格：
  - 周看板、能力雷达、自欺欺人图、合法摆烂、想法的冷宫、罪证封存、体检报告、室友说……
- `alo-copy.ts` 与 `system-copy.ts` 全面重写，空状态、提示、按钮、导航悬停统一调性。
- `monkQuotes.ts` 语录库升级：从温和自嘲到存在主义恐吓，软件不再是一个工具，是一个冷眼旁观你摆烂的室友。

---

## 新增与优化

- **行动台**：新增「快速收集箱」与「收集箱处理器」，支持一键转为 O / KR / 今日任务 / 灵感 / 删除。
- **周看板**：未完成任务跨天自动迁移到今天，并显示「来自昨日」标记；每周一自动清理已完成任务。
- **KR 拖拽**：修复了从 OKR 面板拖到周看板时落点偏移一格的问题，现在指哪打哪。
- **任务编辑**：修复 ability 下拉框选「无」时编辑态意外关闭的 blur bug。
- **系统页**：新增「生存指南」Manual 面板、「数据体检报告」DataHealth 面板、「反思模板」管理面板。
- **更新检查**：改为直接调用 GitHub Releases API，并修正了仓库地址，解决更新检测 404。
- **存储预警**：数据超过 4MB 时控制台警告，超过 4.5MB 时顶部横幅提示导出清理。

---

## 已知问题

- `npm run lint` 仍有少量历史遗留警告（`setState in effect`、显式 `any`），不影响构建与运行，将在后续补丁中清理。

---

## 升级建议

1. 安装前**先导出一份备份**（系统 → 罪证封存 → 导出）。
2. 首次启动 v0.3.0 时，ALO 会自动运行数据迁移；迁移完成后即可正常使用。
3. 如果你之前重度使用 Backlog，迁移后所有 backlog 任务会出现在「今天」，建议花 2 分钟重新分配到本周各天。

---

## 感谢

感谢每一位在深夜打开 ALO、写下反思、勾选任务的自己。软件不会替你完成人生，但至少可以冷静记录你欠了多少。

**—— ASCII Life OS 团队**
